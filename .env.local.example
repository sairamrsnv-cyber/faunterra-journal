# Copy to .env.local and fill in. .env.local is gitignored; this file is not.
#   cp .env.local.example .env.local && chmod 600 .env.local
#
# The scheduled job does NOT inherit your shell, so the token must live here
# rather than only being exported in a terminal.

# Free key: https://ebird.org/api/keygen
#
# Use a DEDICATED Faunterra eBird account — not a personal birding account.
# eBird's terms suspend API abuse at the ACCOUNT level, not the key level, so a
# runaway job on a personal account can cost you access to your own checklist
# history. A project account also survives a handover and gives Cornell a
# Faunterra identity to correspond with about usage or commercial permission.
EBIRD_API_TOKEN=

# Comma-separated eBird region codes. https://ebird.org/region/world
EBIRD_REGIONS=IN

# Where the local archive lives. Defaults to ./ebird-archive.
# Point it at external storage if you prefer. Every tool that writes verifies
# the volume is actually mounted first and refuses if it is not — with the drive
# unplugged, macOS would otherwise let it create /Volumes/LaCie as a folder on
# the boot disk and quietly fill internal storage for weeks.
# EBIRD_ARCHIVE_DIR=/Volumes/LaCie/faunterra/ebird-archive

# Point the tools at something other than eBird. Only for exercising the code
# against the local fixture (desktop/, `cargo run -p ebird-core --bin fixture`).
# Anything written while this is set is stamped "live": false in the archive and
# must not be published.
# EBIRD_API_BASE=http://127.0.0.1:8788/v2
